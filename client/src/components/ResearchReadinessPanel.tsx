import React from "react";
import { BadgeCheck, CircleAlert, Coins, ShieldCheck } from "lucide-react";

export function ResearchReadinessPanel({ canRequest }: { canRequest: boolean }) {
  return <section className="dashboard-card mt-5 border-[#dbe5de] bg-[#fbfdfb]"><div className="flex flex-col gap-4 lg:flex-row lg:items-start lg:justify-between"><div><p className="eyebrow">Research cost and evidence policy</p><h2 className="font-editorial mt-2 text-3xl text-[#193552]">A credit is a commitment, not a button click.</h2><p className="mt-2 max-w-3xl text-sm leading-6 text-slate-600">A completed automated research request is designed to cost <strong>one internal research credit</strong>. Until GenX publishes the documented session web-search, source-citation, and usage contract, saving an evidence-review brief remains at <strong>zero debited credits</strong> and no provider call is made. Those saved briefs can still support an attributable manual review.</p></div><span className={`inline-flex w-fit items-center rounded-full px-3 py-1 text-xs font-bold ${canRequest ? "bg-[#edf5ef] text-[#337f88]" : "bg-[#f5f1ea] text-[#9a691f]"}`}>{canRequest ? "Role can save review briefs" : "Research preparation is role-restricted"}</span></div><div className="mt-5 grid gap-3 md:grid-cols-3"><PolicyCard icon={<Coins className="size-4" />} title="Cost gate" copy="1 credit only after a documented provider dispatch succeeds; the present review-brief queue charges 0." /><PolicyCard icon={<BadgeCheck className="size-4" />} title="Evidence gate" copy="Completed results require reviewed HTTPS sources, retrieval dates, geographic scope and period where applicable." /><PolicyCard icon={<ShieldCheck className="size-4" />} title="Presentation gate" copy="Crime and service evidence is source-bound and never converted into a safety score or verdict." /></div><p className="mt-4 flex items-start gap-2 text-xs leading-5 text-[#9a691f]"><CircleAlert className="mt-0.5 size-3.5 shrink-0" />Automatic provider usage, debit, refund and output are unavailable until the missing GenX contract is officially documented and deployed.</p></section>;
}

function PolicyCard({ icon, title, copy }: { icon: React.ReactNode; title: string; copy: string }) { return <div className="rounded-xl border border-[#e2ebe3] bg-white p-4"><span className="text-[#337f88]">{icon}</span><p className="mt-2 text-sm font-bold text-[#193552]">{title}</p><p className="mt-1 text-xs leading-5 text-slate-600">{copy}</p></div>; }
